const string = 'abca'
var longestString = function(){

const len = string.length;
let stringHash = new Map();
let startPos = 0,i = 0, end = 0, str = "",max = 0;
if(!len) return 0;
if(!len == 1) return 1;
for (let i = 0; i < array.length; i++) {
    const char = string.charAt(i);
    if(!stringHash.get(char)) stringHash.set(char,i);
    else 
        {
             i = stringHash.get(char);
             
        }

        if(max < stringHash.size){

        }
}

}